# Test Fixtures

- `standalone-text.fd` — standalone text node.
- `positioned-text.fd` — text with explicit x/y position.
- `styled-text.fd` — bold larger text styling.
- `text-in-rect.fd` — text nested inside a rect.
- `text-in-ellipse.fd` — text nested inside an ellipse.
- `multiline-text.fd` — text containing actual line breaks.
- `wordwrap-text.fd` — narrow text width to force wrapping.
- `valign-top.fd` — text inside rect with top vertical alignment.
- `valign-middle.fd` — text inside rect with middle vertical alignment.
- `valign-bottom.fd` — text inside rect with bottom vertical alignment.
